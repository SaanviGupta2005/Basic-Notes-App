<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Notes App</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-slate-900">
        <div class="container form px-40 py-10 text-white">
            <h1 class="text-4xl font-bold text-center m-8">Notes App</h1>
            <form action="/create" method="post">
                <input class="block px-5 py-3 bg-slate-800 w-full rounded-lg outline-none " type="text" placeholder="Title" name="title">
                <textarea class="block px-5 py-3 bg-slate-800 w-full rounded-lg outline-none mt-3" placeholder="Task Details..." name="details"></textarea>
                <input class="mt-3 px-5 py-3 bg-blue-600 rounded-lg hover:bg-blue-700 cursor-pointer" type="submit" value="Create Task">
            </form>
        </div>
        <div class="px-40 flex flex-wrap gap-9">
            <% if(files.length > 0) { %>
                <% files.forEach((val)=>{ %>
                    <div class="w-72 px-3 py-4 rounded-lg bg-slate-800">
                        <h1 class="text-white text-2xl"><%= val.title %></h1>
                        <div class="flex justify-between w-full mt-3">
                            <a class="text-blue-500 inline-block" href="/file/<%= val._id %>">Read More...</a>
                            <a class="text-slate-400" href="/edittitle/<%= val._id %>">Edit</a>
                            <a class="text-red-400" href="/delete/<%= val._id %>">Delete</a>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <h2 class="font-semibold text-slate-400 text-2xl">No Notes....</h2>
            <% }  %>
        </div>
    </div>
</body>
</html>