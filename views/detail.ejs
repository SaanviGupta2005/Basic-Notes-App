<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Notes App</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="main w-full min-h-screen bg-slate-900">
        <div class="px-40 py-10 text-white">
            <a class="my-10 px-5 py-3 rounded-lg bg-blue-600 hover:bg-blue-700 cursor-pointer inline-block"
             href="/">Go Back</a>
            <h1 class="text-4xl"><%= note.title %></h1>
            <p class="text-xl mt-8"><%= note.details %></p>
            <a class="my-10 px-5 py-3 rounded-lg bg-yellow-600 hover:bg-yellow-700 cursor-pointer inline-block" 
            href="/editdetails/<%= note._id %>">Update Details</a>
        </div>
    </div>
</body>

</html>