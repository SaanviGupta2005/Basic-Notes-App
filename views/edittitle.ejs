<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Notes App</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="main w-full min-h-screen bg-slate-900">
        <div class="px-40 py-10 text-white">
            <a class="my-10 px-5 py-3 rounded-lg bg-blue-600 hover:bg-blue-700 
            cursor-pointer inline-block" href="/">Go Back</a>
            <form action="/edit/<%= note._id%>" method="post">
                <input class="block px-5 py-3 bg-slate-800 w-2/4 rounded-lg outline-none" 
                 type="text" name="previous" value="<%= note.title %>" readonly>
                <input class="block px-5 py-3 bg-slate-800 w-2/4 rounded-lg outline-none mt-3" 
                type="text" placeholder="New Name" name="new">
                <input class="px-5 py-3 bg-yellow-600 cursor-pointer rounded-lg outline-none mt-5 hover:bg-yellow-700" 
                type="submit" value="Update Title">
            </form>
        </div>
    </div>
</body>

</html>